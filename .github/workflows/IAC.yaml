name: IAC

on:
  push:
    branches: [ "IAC" ]
  pull_request:
    branches: [ "IAC" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Get commit ID
      run: echo "$(git rev-parse --short HEAD)" >tag
